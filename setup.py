from setuptools import setup, find_packages

setup(
    name='RAG_0to1',
    packages=find_packages(),
    url='https://github.com/Ankur-Samanta/RAG_0to1',
    description='RAG implementation from the ground up',
    long_description=open('readme.md').read(),
)